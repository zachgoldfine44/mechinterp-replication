# Core ML
torch>=2.0.0
# transformers is capped at <5.0 because transformer-lens (as of 2.18.0)
# reads attributes like `rope_theta` directly off model config objects in
# a way that breaks with transformers 5.x (Qwen2Config and friends had
# breaking attribute-layout changes). Unpin this once transformer-lens
# publishes transformers-5-compatible releases.
transformers>=4.40.0,<5.0
transformer-lens>=2.0.0
nnsight>=0.3.0

# Interpretability
sae-lens>=4.0.0
einops>=0.7.0
jaxtyping>=0.2.25
# beartype is pinned low because transformer-lens (all versions) caps
# beartype at <0.15.0. If transformer-lens bumps its cap in a future
# release we can raise this bound.
beartype>=0.14.1,<0.15.0
fancy_einsum>=0.0.3

# Data science
pandas>=2.0.0
numpy>=1.24.0
scipy>=1.11.0
scikit-learn>=1.3.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.13.0
plotly>=5.15.0

# Infrastructure
pytest>=7.4.0
pyyaml>=6.0.0
tqdm>=4.65.0
wandb>=0.16.0
huggingface_hub>=0.20.0

# Quantization (for large models)
bitsandbytes>=0.41.0; sys_platform == "linux"

# Critique agents (src/core/critique.py spawns these at the end of each
# model's claim sweep). The Claude SDK is required; the OpenAI SDK is
# optional and will be silently skipped if missing or if OPENAI_API_KEY
# is unset.
anthropic>=0.40.0
openai>=1.50.0
